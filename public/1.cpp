//sahil_lohar_nitr
#include<bits/stdc++.h>
using namespace std;
#define space
#define ll long long int
#define all(arr) arr.begin(), arr.end()
#define add(arr) accumulate(arr.begin(), arr.end(),0)
#define max_ele(arr) *max_element(all(arr))
#define min_ele(arr) *min_element(all(arr))
#define lcm(a,b) (a/gcd(a,b))*b
#define gcd(a,b) __gcd(a,b)
#define count(a,x) count(a.begin(), a.end(),x)
#define N 1000000007 // 10^9+7
#define take(n) int n; cin>>n;
#define gets(s) string s; cin>>s;
#define take_vector(a) for(auto &x:a)cin>>x;
#define print_vector(a) for(int i=0; i<a.size(); i++)cout<<a[i]<<space;
#define take_array(a,n) for(int i=0;i<n;i++)cin>>a[i];
#define print_array(a,n) for (int i = 0; i < n; i++)    cout << a[i] << space;
#define take_matrix(a,n,m) for(int i=0; i<n; i++)for(int j=0; j<m; j++)cin>>a[i][j];
#define print_matrix(a,n,m) for(int i=0; i<n; i++){for(int j=0; j<m; j++)cout<<a[i][j]<<space;cout<<endl;}
 
int func()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  
  return 0;
}
int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  take(t)
  while ( t--)
       func();
  return 0;
}